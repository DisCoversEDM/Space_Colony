<?xml version="1.0" encoding="utf-8"?>
<!-- bottom_sheet_building.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_bottom_sheet"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="20dp"
    android:paddingBottom="32dp">

    <!-- Drag handle -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="@color/border_muted" />

    <!-- Header row: icon + name + capacity -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:gap="12dp">

        <ImageView
            android:id="@+id/iv_building_icon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_building_hospital"
            android:tint="@color/teal_400"
            tools:ignore="ContentDescription"
            xmlns:tools="http://schemas.android.com/tools" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_building_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hospital"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/text_md"
                android:fontFamily="@font/exo2_medium" />

            <TextView
                android:id="@+id/tv_building_capacity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2 / 3 capacity"
                android:textColor="@color/text_hint"
                android:textSize="@dimen/text_xs" />
        </LinearLayout>

        <!-- Upgrade progress (visible while builder working) -->
        <LinearLayout
            android:id="@+id/layout_upgrade_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:visibility="gone">

            <TextView
                android:id="@+id/tv_upgrade_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Upgrading…"
                android:textColor="@color/amber_400"
                android:textSize="@dimen/text_xs" />

            <ProgressBar
                android:id="@+id/progress_building_upgrade"
                style="@style/Widget.AppCompat.ProgressBar.Horizontal"
                android:layout_width="80dp"
                android:layout_height="4dp"
                android:progressDrawable="@drawable/progress_upgrade"
                android:max="100"
                android:progress="40" />
        </LinearLayout>
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="14dp"
        android:background="@color/border_default" />

    <!-- Crew assigned to this building -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Crew here"
        style="@style/TextSectionHeader"
        android:layout_marginBottom="8dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_building_crew"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="false" />

    <Button
        android:id="@+id/btn_upgrade_building"
        android:layout_width="match_parent"
        android:layout_height="@dimen/action_button_height"
        android:layout_marginTop="16dp"
        android:text="@string/building_upgrade"
        style="@style/ButtonSecondary" />

</LinearLayout>
